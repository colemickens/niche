package main

import "testing"

func TestEndToEndAzure(t *testing.T) {
	// generate a known key
	// call `config init`
	// call `download`, `upload`
	// call `build` and see if we get new files?
	// - but build is going to try to hit the network
}
